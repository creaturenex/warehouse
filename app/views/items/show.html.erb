<p style="color: green"><%= notice %></p>

<%= render @item %>

<div>
  <%= link_to "Edit this item", edit_inventory_item_path(@inventory, @item) %> |
  <%= link_to "Back to items", inventory_items_path(@inventory) %>

  <%= button_to "Destroy this item", @item, method: :delete %>
</div>
